{
  "name": "zap-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ zapproject/zap-subgraph",
    "create-local": "graph create --node http://localhost:8020/ zapproject/zap-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ zapproject/zap-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 zapproject/zap-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.20.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}

